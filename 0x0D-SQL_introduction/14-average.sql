-- Computes the score average of all records in the table second_table of the database hbtn_0c_0,by Okpako Michael
-- The result column name should be an average
SELECT AVG(score) AS average
FROM second_table;
